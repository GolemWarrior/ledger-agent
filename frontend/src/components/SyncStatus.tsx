export default function SyncStatus() {
  // FR-35: Sync status banner + manual sync button — implemented in Story 1.4
  return (
    <span className="text-sm text-gray-400">Sync — Story 1.4</span>
  )
}
